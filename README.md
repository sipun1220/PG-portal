# PG-portal

## Automatic language translation

The language selector supports English, Punjabi, Odia, Telugu, Tamil, Hindi, and Bengali through the free Google Translate Website Widget. It requires no API key. Google translates the rendered page, including content added dynamically by the browser.

The widget requires internet access and may have less control over translation quality and styling than a paid translation API. English is the default language.